#include <iostream>
using namespace std;

using point =  pair<int,int> ;

int main() {
    point a,b,c,d,e,f;
    float size;
    cin>>a.first>>a.second>>a.first>>b.second>>b.first>>c.second>>c.first>>d.second>>d.first>>d.second>>e.first>>e.second>>f.first>>f.second;
    cout<<size;
}
// 64 位输出请用 printf("%lld")